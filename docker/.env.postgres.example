# ============================================
# PostgreSQL Configuration
# ============================================
# Este arquivo contém as configurações do banco de dados PostgreSQL
# 
# Para usar este arquivo:
# 1. Copie para .env.postgres: cp .env.postgres.example .env.postgres
# 2. Ajuste os valores conforme necessário
# 3. IMPORTANTE: Altere as senhas em produção!

# Usuário do banco de dados
POSTGRES_USER=postgres

# Senha do banco de dados
# ⚠️ ALTERE ESTA SENHA EM PRODUÇÃO!
POSTGRES_PASSWORD=postgres

# Nome do banco de dados
POSTGRES_DB=voto_inteligente

# Porta do PostgreSQL (padrão: 5432)
# Esta porta é mapeada no docker-compose.yml
POSTGRES_PORT=5432
