# ============================================
# Backend Configuration (NestJS)
# ============================================
# Este arquivo contém as variáveis de ambiente do backend
# 
# Para usar este arquivo:
# 1. Copie para .env.backend: cp .env.backend.example .env.backend
# 2. Ajuste os valores conforme necessário
# 3. IMPORTANTE: Altere os secrets em produção!

# ============================================
# Server Configuration
# ============================================
# Porta em que o servidor backend irá rodar
PORT=4000

# Ambiente de execução (development, production, test)
NODE_ENV=production

# ============================================
# Database Configuration
# ============================================
# A DATABASE_URL é configurada automaticamente pelo docker-compose.yml
# baseada nas variáveis do .env.postgres
# Formato: postgresql://user:password@host:port/database?schema=public
# DATABASE_URL será sobrescrita pelo docker-compose.yml

# ============================================
# Redis Configuration
# ============================================
# Redis é configurado automaticamente pelo docker-compose.yml
# REDIS_HOST=redis
# REDIS_PORT=6379

# ============================================
# JWT Configuration (quando implementar autenticação)
# ============================================
# Secret para assinar tokens JWT
# ⚠️ ALTERE ESTE VALOR EM PRODUÇÃO! Use um valor seguro e aleatório
# Você pode gerar um secret seguro com: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Tempo de expiração do token JWT (em segundos)
# Padrão: 7 dias (604800 segundos)
JWT_EXPIRES_IN=604800

# ============================================
# CORS Configuration
# ============================================
# Aliases para CORS (configurados automaticamente pelo docker-compose.yml)
# FRONTEND_ALIAS será configurado pelo docker-compose.yml
# BACKEND_ALIAS será configurado pelo docker-compose.yml

# ============================================
# Application Configuration
# ============================================
# URL base da aplicação (para links em emails, etc.)
APP_URL=http://localhost:4000

# ============================================
# Logging
# ============================================
# Nível de log (error, warn, info, debug, verbose)
LOG_LEVEL=info
