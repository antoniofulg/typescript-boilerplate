# Docker Compose Project Name
# Used to isolate multiple clones of the same repository
# Each clone should have a unique project name to avoid conflicts
# Copy this file to .env and customize
COMPOSE_PROJECT_NAME=voto-inteligente

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=voto_inteligente_db
POSTGRES_PORT=5432

# Redis Configuration
REDIS_PORT=6379

# Backend Configuration
BACKEND_PORT=4000
PRISMA_STUDIO_PORT=5555

# Frontend Configuration
FRONTEND_PORT=3000

# Host Aliases (for /etc/hosts)
FRONTEND_ALIAS=voto-inteligente.frontend.local
BACKEND_ALIAS=voto-inteligente.backend.local

