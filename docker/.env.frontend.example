# ============================================
# Frontend Configuration (Next.js)
# ============================================
# Este arquivo contém as variáveis de ambiente do frontend
# 
# Para usar este arquivo:
# 1. Copie para .env.frontend: cp .env.frontend.example .env.frontend
# 2. Ajuste os valores conforme necessário
# 
# IMPORTANTE: Variáveis que começam com NEXT_PUBLIC_ são expostas ao cliente
# e devem conter apenas informações públicas. NÃO coloque secrets aqui!

# ============================================
# Server Configuration
# ============================================
# Porta em que o servidor frontend irá rodar
PORT=3000

# Ambiente de execução (development, production, test)
NODE_ENV=production

# ============================================
# API Configuration
# ============================================
# URL do backend API
# Esta variável é exposta ao cliente (NEXT_PUBLIC_)
# Será configurada automaticamente pelo docker-compose.yml baseado no BACKEND_ALIAS
# NEXT_PUBLIC_BACKEND_URL será configurado pelo docker-compose.yml

# ============================================
# Application Configuration
# ============================================
# Nome da aplicação
NEXT_PUBLIC_APP_NAME=Full-Stack Boilerplate

# URL base da aplicação (para links, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Feature Flags (opcional)
# ============================================
# Use feature flags para habilitar/desabilitar funcionalidades
# NEXT_PUBLIC_ENABLE_ANALYTICS=false
# NEXT_PUBLIC_ENABLE_DEBUG_MODE=false

# ============================================
# Aliases (configurados automaticamente)
# ============================================
# FRONTEND_ALIAS será configurado pelo docker-compose.yml
# BACKEND_ALIAS será configurado pelo docker-compose.yml
