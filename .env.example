# ============================================
# Full-Stack Boilerplate - Environment Variables
# ============================================
# This file contains environment variables used by the Makefile
# and can be used throughout the project
# 
# To use this file:
# 1. Copy to .env: cp .env.example .env
# 2. Adjust values as needed
# 3. The .env file is in .gitignore and will not be committed

# ============================================
# Host Aliases Configuration
# ============================================
# These aliases will be added to the /etc/hosts file during build
# and allow accessing services via custom URLs
# 
# Usage example:
# - Frontend: http://app.frontend.local:3000
# - Backend:  http://app.backend.local:4000

# Alias for frontend (default: app.frontend.local)
FRONTEND_ALIAS=app.frontend.local

# Alias for backend (default: app.backend.local)
BACKEND_ALIAS=app.backend.local

# ============================================
# Docker Compose Overrides (opcional)
# ============================================
# You can override docker-compose.yml variables here
# Estas variáveis serão passadas para os containers

# Example: To use different aliases
# FRONTEND_ALIAS=meu-frontend.local
# BACKEND_ALIAS=meu-backend.local
